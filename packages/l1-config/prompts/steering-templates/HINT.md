# L1-T18 · steering 措辞模板 baseline（被进化基质）

# HINT 软提示模板（更可行动 = adoption↑ 目标）。
HINT: |
  Steering HINT: consider {suggestion}. Reflect on whether the current step
  aligns with the intended direction before continuing.

# REDIRECT 重定向模板（须先 abort in-flight child）。
REDIRECT: |
  Steering REDIRECT: abort the current in-flight step, then proceed with
  {new_intent}. Do not complete the in-flight action.
